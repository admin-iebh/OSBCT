# h3 — the display column made per-page, and `withCOL` landed behind a caesura measurement

Working record.  `cA` = per-volume column (PAGEFID_PAGECOL=0), `cB` = per-page column, both
on the corpus as it stood at `1757a61a`.  `cG` = per-page column on the corpus after `C2COL`.
`cFinal` = cB overlaid with cG (all 118 volumes, final state).
`rA/rB/rB2/rC/rE` = row dumps.  `TASKA.txt`, `TASKB_FINAL.txt` = the two censuses.
Backups: `pipeline/{build_khu_volume,check_page_fidelity}.py.preDC`,
`site/reader/*/<VOL>.json.preDC` (133 files, 19 volumes + 09DiT02 + 38Abhi10).
